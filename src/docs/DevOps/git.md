# Git Workflow Guide

Essential Git commands and workflows for team collaboration.

## Basic Commands

```shell
git init
git add .
git commit -m "message"
git push origin main
```

## Branch Management

```shell
git checkout -b feature/new-ui
git merge main
git branch -d feature/new-ui
```